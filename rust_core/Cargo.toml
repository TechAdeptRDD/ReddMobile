[package]
name = "rust_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_core"
crate-type = ["cdylib"]

[dependencies]
# We will add actual crypto dependencies (secp256k1, bip39) in the next phase.
# For now, we just need the compiler to successfully build the FFI bridge.
