# app

Flutter frontend workspace for ReddMobile.

## Scope

- Flutter UI and app state management.
- Local persistence using Isar Database.
- Integration points that consume the `api` client and `core` cryptography features.
